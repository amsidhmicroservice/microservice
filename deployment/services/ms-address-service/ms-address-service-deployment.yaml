apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: address-ms
    version: 0.0.2-apm
  name: address-ms
  namespace: apps
spec:
  replicas: 1
  minReadySeconds: 45
  selector:
    matchLabels:
      app: address-ms
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: address-ms
        version: 0.0.2-apm
    spec:
      containers:
        - image: amsidhmicroservice/ms-address-service:0.0.2-apm
          imagePullPolicy: IfNotPresent
          name: address-ms
          ports:
            - name: tomcat-port
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: address-ms-configmap
            - secretRef:
                name: address-secret
          livenessProbe:
            httpGet:
              path: "/actuator/health/liveness"
              port: tomcat-port
            failureThreshold: 5
            periodSeconds: 10
            initialDelaySeconds: 20
            timeoutSeconds: 10
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: "/actuator/health/readiness"
              port: tomcat-port
            failureThreshold: 5
            periodSeconds: 10
            initialDelaySeconds: 20
            timeoutSeconds: 10
            successThreshold: 1

          resources:
            requests:
              memory: 512Mi
            limits:
              memory: 1024Mi
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
