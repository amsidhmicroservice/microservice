apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: location-ms
    version: 0.0.2-SNAPSHOT
  name: location-ms
  namespace: apps
spec:
  replicas: 1
  minReadySeconds: 45
  selector:
    matchLabels:
      app: location-ms
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: location-ms
        version: 0.0.2-SNAPSHOT
    spec:
      containers:
        - image: amsidhmicroservice/ms-location-service:0.0.2-SNAPSHOT
          imagePullPolicy: IfNotPresent
          name: location-ms
          ports:
            - name: tomcat-port
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: location-ms-configmap
            - secretRef:
                name: location-secret
          livenessProbe:
            httpGet:
              path: "/actuator/health/liveness"
              port: tomcat-port
            failureThreshold: 5
            periodSeconds: 30
            initialDelaySeconds: 180
            timeoutSeconds: 30
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: "/actuator/health/readiness"
              port: tomcat-port
            failureThreshold: 5
            periodSeconds: 10
            initialDelaySeconds: 20
            timeoutSeconds: 10
            successThreshold: 1

          resources:
            requests:
              memory: 512Mi
            limits:
              memory: 1024Mi
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
