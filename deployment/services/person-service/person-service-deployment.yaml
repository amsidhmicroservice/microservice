apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: person-ms
    version: latest
  name: person-ms
  namespace: default
spec:
  replicas: 1
  minReadySeconds: 45
  selector:
    matchLabels:
      app: person-ms
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: person-ms
        version: latest
    spec:
      containers:
        - image: amsidhmicroservice/person-service:latest
          imagePullPolicy: IfNotPresent
          name: person-ms
          ports:
            - name: liveness-port
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: person-ms-configmap
          resources:
            requests:
              memory: 512Mi
            limits:
              memory: 1024Mi
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: person-service
    version: latest
  name: person-service
spec:
  ports:
    - name: http
      port: 8181
      targetPort: 8080
      nodePort: 32110
      protocol: TCP
  selector:
    app: person-ms
    version: latest
  type: LoadBalancer
