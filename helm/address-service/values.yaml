namespace: default
version: 0.0.4-SNAPSHOT

deployment:
  imageName: address-service
  name: address-ms
  numberOfReplicas: 1

service:
  name: address-service
  type: NodePort

configmap:
  activeProfile: prod

