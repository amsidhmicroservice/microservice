namespace: default
version: 0.0.4-SNAPSHOT

deployment:
  imageName: location-service
  name: location-ms
  numberOfReplicas: 1

service:
  name: location-service
  type: NodePort

configmap:
  activeProfile: prod

